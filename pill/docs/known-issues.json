{
 "standard": "every image EXACT or listed here with mechanism+status; an unlisted miss is a triage bug",
 "issues": {
  "shiny/s-0bfc44d8.jpg": {
   "status": "near-fixed",
   "note": "Count 33 v 34, per-pill 31/34 matched with only 2 spurious (was 24/34 with 9). Two fixes: (1) splotch filter was killing 13 real beads — population sanity guard; (2) unitLen collapsed 53 vs true ~83 because the beads read as ON-EDGE (displaced-area ratio 0.665, the documented on-edge 2/3), and the shape-vouched override guards only described DEBRIS collapse, so it never fired — added an on-edge band arm. NOTE two of my diagnoses were WRONG and corrected by measurement: the template pool does NOT collapse (31 of 43 blobs, tplMajor 69.9 — solidity is roughness-TOLERANT, only circularity is quadratic in perimeter), and the misses were never mask loss. REMAINING: 3 misses + offset/undersized outlines, both from the mask cutting the dark half of each bead before geometry runs — a mask-stage defect, not a scale one."
  },
  "lined/lined-503b3041.jpg": {
   "status": "detection-improved-count-open",
   "note": "ROOT CAUSE FOUND (was mis-attributed to ruled-line bridging): the fused cluster took the deep-pile seeding branch, whose premise \"each pill rises to its own DT peak\" is FALSE for a flat sheet — 81428px produced only 16 strict-max seed pixels, so the watershed could not split it and the count came from area estimation. Fix: reseed at pill scale when a blob is seed-starved AND single-layer (DT/radius < 2.4x; real stacking measures 3.3x) AND >= 8 pills. Per-pill detection: 503b 6/20->13/20, bfdbfef9 8/20->16/20. Counts unchanged (18/18/21) — the remaining gap is splitting accuracy inside the reseeded cluster."
  },
  "lined/lined-69204ff4.jpg": {
   "status": "detection-improved-count-open",
   "note": "ROOT CAUSE FOUND (was mis-attributed to ruled-line bridging): the fused cluster took the deep-pile seeding branch, whose premise \"each pill rises to its own DT peak\" is FALSE for a flat sheet — 81428px produced only 16 strict-max seed pixels, so the watershed could not split it and the count came from area estimation. Fix: reseed at pill scale when a blob is seed-starved AND single-layer (DT/radius < 2.4x; real stacking measures 3.3x) AND >= 8 pills. Per-pill detection: 503b 6/20->13/20, bfdbfef9 8/20->16/20. Counts unchanged (18/18/21) — the remaining gap is splitting accuracy inside the reseeded cluster."
  },
  "lined/lined-bfdbfef9.jpg": {
   "status": "detection-improved-count-open",
   "note": "ROOT CAUSE FOUND (was mis-attributed to ruled-line bridging): the fused cluster took the deep-pile seeding branch, whose premise \"each pill rises to its own DT peak\" is FALSE for a flat sheet — 81428px produced only 16 strict-max seed pixels, so the watershed could not split it and the count came from area estimation. Fix: reseed at pill scale when a blob is seed-starved AND single-layer (DT/radius < 2.4x; real stacking measures 3.3x) AND >= 8 pills. Per-pill detection: 503b 6/20->13/20, bfdbfef9 8/20->16/20. Counts unchanged (18/18/21) — the remaining gap is splitting accuracy inside the reseeded cluster."
  },
  "real/r-7ff7fd99.jpg": {
   "status": "open-real-miss",
   "note": "LEDGER CORRECTED: the \"shingled pill\" verdict was WRONG (mine). 25x crop shows TWO separate caplets meeting at a dark contact seam, each with its own ground shadow — flat, side-by-side. Centers 20.5px apart vs a 20px pill width = flush coplanar contact, not the 7px offset stacking would give. This is a genuine flush-pair split failure, NOT a scope question. Per-pill now 18/20 (2 miss, 1 spurious) after the sibling-overlap fix."
  },
  "synth2-rc-light-small-n30-t65-s140.png": {
   "status": "awaiting-ruling",
   "note": "camouflage class-C: per-pill random colours violate one-medication; regen ruling"
  },
  "synth2-rc-light-normal-n30-t0-s144.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-light-large-n30-t35-s158.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-gradient-small-n12-t0-s161.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-gradient-small-n30-t65-s170.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-gradient-normal-n120-t65-s184.png": {
   "status": "label-dispute",
   "note": "annotation 106 vs manifest 104 - owner re-audit"
  },
  "synth2-rc-noise-small-n60-t65-s201.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-wood-large-n30-t35-s248.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-wood-small-n12-t0-s221.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-kraft-normal-n12-t65-s271.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-kraft-large-n30-t35-s278.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "synth2-rc-kraft-large-n12-t65-s279.png": {
   "status": "awaiting-ruling",
   "note": "camo class-C"
  },
  "shiny/s-eb90778f.jpg": {
   "status": "open-detection",
   "note": "LABEL CORRECTED 34 (was 37 — mine, wrong). This image was previously reported as \"37 EXACT\" and shipped as a win; against the true label it is +3 OVER with 6 spurious (per-pill 31/34). The chroma-rescue \"5 beads recovered\" claim was measured against the wrong target. Both shiny images now have per-pill centres so this cannot recur."
  }
 },
 "guards": {
  "high-contrast-mask-rescue": {
   "note": "Trigger has exactly ONE positive example in 251 images (cream). Force-running the rescue with the trigger bypassed: 35 adoptions, 2 BREAKS including cornerstone s301 20->40. The explain-margin cannot separate them (cream +0.204 sits between harmful +0.165 and +0.489), so the UPSTREAM TRIGGER is load-bearing for safety, not arbitration. Do not loosen the trigger without re-running that force-test."
  },
  "gate-blindspot": {
   "note": "CLOSED 2026-08-11 by tools/perpill.mjs (baseline tools/baselines/perpill.json, 407/444 matched, 37 missed, 24 spurious over 24 annotated images). The count gate is blind to detection quality — proven when two agent patches that would have silently reverted shipped per-pill fixes both gated GREEN on counts. Run BOTH gates before any counter commit."
  },
  "perpill-blind-on-shiny": {
   "note": "testdata/centers/ has NO annotations for the two shiny photos, so tools/perpill.mjs cannot see mask/detection quality there — exactly the images where counts are least trustworthy. Hand-annotate them before the next shiny attempt."
  },
  "matchquality-scale-bias": {
   "note": "matchQuality REWARDS UNDERSIZED outlines: sweeping a scale multiplier over shipped placements, q peaks at 0.85 on shiny/s-0bfc44d8 (visibly undersized) and only 1.00 on cream-caplets (visually correct). Mechanism: the rim edge test samples +/-1.8px (EDGE_OFF), so on a bead fading gradually from bright centre to dark edge an outline drawn INSIDE the pill still finds a local step and scores well. Consequence: the metric must NOT drive major/minor refinement until this is fixed — optimising size against it would visibly worsen the owner's standing complaint. Fix direction: scale the edge offset to pill size and/or require the step at the TRUE silhouette (largest gradient along the radial ray), not any step."
  },
  "matchquality-needs-calibrators": {
   "note": "matchQuality discrimination tracks calibration pool size: pool >=11 gives AUC 0.71-1.00 (shiny 0.90, r-f5d11815 1.00), pool <=4 gives 0.56-0.63 and on lined-503b3041 it INVERTS (bad cross-pill lassos 0.616 vs true pills 0.571). Lined images have only ~4 verified singles because the pile is one blob. A canJudge guard (pool >= 11) refuses to flag on a weak calibrator rather than dash outlines at random."
  },
  "synthetic-corpus-must-be-single-medication": {
   "note": "tools/gen-pills.html drew a NEW RANDOM COLOUR PER PILL until 2026-08-12 (drawRound: `light = PILL==='white' || rand()<0.6` then a 3-colour pick; drawCapsule: `CAP_COLORS[(rand()*4)|0]`), both INSIDE the draw loop. That produced green+white and white/pink/orange pills in a single frame — a direct violation of the owner's scope rule that a photo shows ONE medication (same size AND shape AND colour scheme; a consistent two-tone split is fine, per-pill random colours are OUT of scope). 186 of the corpus's synthetics were affected; 174 of them PASSED anyway, purely on a lucky palette draw, while the 12 that failed did so for a scope reason, not an algorithm reason — the counter was correctly refusing to unify several different-coloured pill populations. Fixed by hoisting both colour draws OUT of the loop (ROUND_BASE / CAP_PAIR, chosen once per image from the seed) and regenerating all 186 with exact generator-emitted truth. DO NOT move a colour draw back inside a draw loop: it silently makes the corpus test something the product does not claim to do, and it hides real regressions behind luck."
  },
  "glare-pulls-centres-toward-highlight": {
   "note": "MEASURED 2026-08-12 on s-0bfc44d8. Placement centres on glossy pills are systematically displaced TOWARD THE SPECULAR HIGHLIGHT: mean displacement (-0.50, -7.91) px — horizontal component ~0, vertical -7.9px, i.e. 28% of the pill WIDTH (median offset 8.8px). The bias tracks gloss, not code: s-0bfc44d8 -7.91, s-eb90778f -3.37 (both shiny, both UP toward the light), vs r-7ff7fd99 -0.26 and r-295482c1 +1.17 (matte, no bias, opposite signs). A coordinate/scale bug would bias every image alike; this does not. MECHANISM: the shadowed LOWER rim falls below the foreground threshold and is lost. Mask coverage above vs below the pill centre is 35.5%/27.8% on shiny but 41.1%/42.3% on matte, and stages/mask-final.png shows blobs bitten off at the bottom, several shedding a detached crumb where the dark rim breaks up. IMPORTANT SCOPE: the mask truncation accounts for only ~3px of the ~7.9px (mask centroid dy = -2.98). The remaining ~5px comes from the stamp fitting the truncated blob. Fixing the mask is NECESSARY BUT NOT SUFFICIENT — do not expect the offset to vanish by widening the threshold, and re-measure both numbers after any change."
  },
  "regress-baseline-is-untracked-and-self-overwrites": {
   "note": "tools/regress.mjs WRITES tools/baselines/counts.json on every run, and that file is UNTRACKED — so a run of a bad build silently installs the bad numbers as the baseline, and the next comparison blesses a regression. Hit for real 2026-08-12: a rim-recovery experiment measured 218/251 and then 215/251 against a shipping 237/251, and left the baseline reading 215. Before trusting any comparison, check tools/baselines/counts.json's summary against the last KNOWN-GOOD gate number recorded in a commit message. SECOND TRAP, same day, cost six runs: piping regress.mjs through `tail` loses all output (it buffers to completion, and the wrapper reports an empty file that looks exactly like a silent crash). Pipe through `tee <file>` instead — the gate was never broken, the invocation was."
  },
  "counts-can-be-right-for-wrong-reasons": {
   "note": "MEASURED 2026-08-12 by tools/geomgate.mjs across the corpus: 407 overlapping placement pairs, 142 duplicates, 160 off-pill, in 65 of 219 images — MANY OF WHICH COUNT PERFECTLY. Cornerstone example synth2-rc-noise-small-n12-t65-s199.png: count 12/12 EXACT, but per-pill 8/12 with 4 MISSED and 4 SPURIOUS. The overlay shows a heap of stacked orange rings on a few beads while four real pills sit ringed red beside them. The errors cancel, so every count-based gate reports success. This is the 'lucky, not detecting' mode the stamp strategy exists to kill, and it is invisible to regress.mjs by construction. Run BOTH tools/geomgate.mjs --check AND tools/perpill.mjs --check before believing a counter change is good; a count-only green is not evidence. The failures cluster hard in dense touching sets (-small- with t65 = 65% touching), which is where placements have the least room and the partition is least constrained. QUANTIFIED 2026-08-12 across the full corpus: of the 20 images with geometry violations, SIXTEEN COUNT EXACTLY. The dominant signature is equal-and-opposite error — synth2-rc-dark-small-n60-t65-s111 is 60/60 EXACT with per-pill 53/60 (7 missed, 7 spurious, 57 overlaps, 25 duplicates), and s199 is 12/12 EXACT with 8/12 (4 missed, 4 spurious). Phantoms cancel misses inside dense touching clusters, so the count is a coincidence. The pose refiner fix moved the corpus 407 -> 389 overlapping pairs (4%), which is real but marginal: it can only re-pose pills, and the defect is that the wrong NUMBER of pills is created inside one blob upstream."
  },
  "adversarial-suite-exposes-hex-raft-collapse": {
   "note": "tools/gen-adversarial.{html,mjs} builds 36 deliberately hostile layouts with EXACT generator truth (hexraft/chain/ring/pair/cross/lonely/dense/bimodal). First run 2026-08-12: 10/36 exact overall, and on the FAIR subset (flat light/dark backgrounds, uniform pills, no noise) 6/19 exact with per-pill 78/384. The corpus completely hid this — it is mostly random scatter, so hard packings only ever arrived by accident. Headline failure: a HEX-PACKED RAFT of 19 obvious pills on a flat background returns count=1. The distance transform shows a SINGLE bright mound with faint spokes; touching pills leave no background between them, the DT never dips, and watershed yields one basin. Fails identically at touch=0.96 (overlapping) and touch=1.04 (2% apart), so it is not a knife edge. chain and lonely score 100% — the defect is specifically 2-D packing, not contact per se. CAVEAT on the noise cases: the generator's confetti background is far harsher than any real photo and produced count=466 on a 7-pill image; treat noise-background results as generator artefacts until the background is made realistic, and judge progress on the light/dark/wood subset."
  },
  "scale-estimate-is-the-keystone-on-hard-packings": {
   "note": "MEASURED 2026-08-12 by tools/raft-bakeoff.mjs on the adversarial suite. The raft failure was NOT primarily a segmentation problem — it was a SCALE problem. The pill radius is read from the DT ridge, which is only as good as the mask, and on a LIGHT board a pale pill barely clears the threshold: measured R=6.7 against a true 13 (half scale) while identical pills on a dark board measured 10.5. Every downstream technique then searches at the wrong scale and over-proposes 3-5x. Estimating scale from the PHOTO's autocorrelation (first peak = centre-to-centre pitch = 2R, which never consults a threshold) fixed R to 13.0 and improved EVERY technique at once: dt recall 45.5% -> 81.3%, hough 16 -> 21 exact, consensus spurious 248 -> 60. Every case where R comes out correct is now EXACT, including hexraft 37/37 and dense 120/120. All 6 remaining failures have R ~6-7 — the autocorrelation is silent when pills do NOT repeat (lonely/pair/chain on a light board), and an area-based fallback for those measured worse (21 -> 19 exact) because a merged raft's area implies a huge radius. Scale on a low-contrast background with non-touching pills is the open problem."
  },
  "geom-baseline-self-overwrites": {
   "note": "Running `node tools/geomgate.mjs <one-image>` REWRITES tools/baselines/geom.json with that single image's numbers, silently replacing the corpus-wide baseline. It happened during the cluster-solver work: a one-image check left images:1 in the file and the next comparison read it as if it covered 219, which made a solver improvement look like a regression. Same class as the regress.mjs counts.json trap. For attribution, do a head-to-head instead: VALEYE_NO_CLUSTER=1 node tools/geomgate.mjs vs the plain run, same build. Measured that way the solver is 197->80 overlapping pairs, 59->16 duplicates, 85.1->39.8px worst penetration."
  },
  "compare-only-against-a-fresh-run": {
   "note": "A stored gate output is a snapshot of the build that produced it, and comparing a new run against a stale one manufactures phantom regressions. Hit TWICE: (1) tools/baselines/geom.json overwritten by a single-image run; (2) /tmp/geom-full.txt from Aug 13 compared against an Aug 17 run, which read as \"physics made it 80 -> 145 overlapping pairs\" until s111 was checked out at the pre-physics commit and measured IDENTICAL (17 pairs, 7 duplicates). Both physics changes were exonerated. Rule: for attribution, run BOTH arms on the SAME build in the same session (VALEYE_NO_CLUSTER=1 vs plain), and never trust a baseline file whose mtime you have not read."
  },
  "cluster-solver-measured-three-ways": {
   "note": "Cluster solver attribution, all three arms run on ONE build (183f7b3): solver OFF = 197 overlapping pairs / 59 duplicates / 85.1px worst over 59 of 219 images; solver ON with deletion BLOCKED = 145 / 44 / 43.1px over 48; solver ON with deletion ALLOWED = 80 / 16 / 39.8px over 45. So the solver is -59% pairs and -73% duplicates, and blocking deletion was silently costing 65 pairs and 28 duplicates. off-pill rises 6 -> 19: separating two stacked placements leaves one off its material, which is a visibly wrong outline traded for an invisible miscount. If a future change makes the solver look weaker, check whether something is rejecting its deletions before concluding the solver regressed."
  },
  "log-blob-is-not-a-detector": {
   "note": "TRIED AND REJECTED: Laplacian-of-Gaussian at the known pill radius as a detector. In the region bake-off it posts the highest recall of any technique (97-100% on shiny/lined/real, and it is the ONLY technique that finds anything on lined paper where otsu and dt-peaks both score 0%), which makes it look like the answer. It is not: the ranking is worthless. On shiny/s-eb90778f the worst true pill ranks #4412 of 115328 peaks, so keeping the top 34 peaks recovers 3 of 34 pills. A shape score (disc uniformity x annulus contrast) was tried as a re-ranker and measured WORSE still: 0/34 in the top 34. The high recall is an artefact of generating ~115k candidates for 34 pills -- at that density almost anything is \"found\". Do not revisit without a fundamentally different candidate reduction."
  },
  "lone-witness-cannot-win-the-panel": {
   "note": "RECURRING DEAD END, reached three times from different directions. On lined-bfdbfef9 blob 1 the votes are ws:19 mass:14 crease:10 ero:3; truth is ~14, so mass alone is right, and the panel discards it because ks.length>=2 requires two agreeing methods. The same shape appears on adv-dense-light (ws:126, truth 120, base 139 wins). It is tempting every time. MEASURED ANSWER (tools/panel-audit.mjs, 128 disagreeing blobs): base is closest on 72 blobs, mass on 13, ws on 5. On the wide-divergence slice (base vs mass differ >=1.5x, 20 blobs) base still wins 12 to 8. Letting a lone witness win loses far more than it gains. Note also that k >= regs.length and corroboratedDescent contradict each other -- the floor always wins -- but that is NOT the operative blocker: measured over six failing images, descent-blocked fired 0 times because ks.length>=2 fails first."
  },
  "bright-percentile-cue-costs-ten-images": {
   "note": "TRIED TWICE AND REVERTED: adding a bright-percentile map as a third entrant to the cue retry, for pale pills on textured wood. The DIAGNOSIS is solid -- on wood the histogram is trimodal (pills ~250, pale grain ~180-200, dark grain ~120) so Otsu splits pale grain from dark grain and the mask comes out as horizontal GRAIN BANDS; cutting at the 99th luminance percentile recovers the pills exactly (measured coverage 9/9, 19/19, 8/8, 8/8, with the threshold jumping 129 -> 245 and nothing between). ATTEMPT 1, binary mask: worse -- distBg is a distance-like map and Otsu/DT/watershed all read gradient, so a hard 0/255 broke them (hexraft-wood 19 -> 15, stack 7 -> 5). ATTEMPT 2, graded distance-above-board: fixes hexraft-wood to 19/19 EXACT but costs the CORPUS ten images (239 -> 229). The coherence score lets it win on photos where the default map was fine. Any future attempt must gate on something that identifies a textured board specifically, not on the coherence score alone -- and must be measured against the full corpus, since the adversarial wood cases all improve while real photos collapse."
  },
  "dense-board-count-vs-picture": {
   "note": "The 120-pill dense board counts 142 while finding ALL 120 pills (0 missed, 22 spurious). Three measured attempts, all reverted: (1) IoU cut -- phantom median IoU 0.509 is IDENTICAL to real, because a phantom between two real pills still covers pill material. No threshold separates them. (2) Non-overlap cut -- phantom median penetration 14.35px vs real 3.71px, a 4x signal, but real pills reach 24.68px so no single cut works (pen>4px drops all 22 phantoms AND 50 real). (3) Prefer ws on photo-seeded blobs -- the evidence is strong (ws closer on 6 of 6, total error 22 vs base 70) and counts improve corpus-wide (dense 142->129, adversarial 35->36 exact, spurious 170->130), BUT the picture collapses: lowering k does not re-place the region, so dense went from 120/120 pills found to 7/120 and adversarial per-pill fell 719->577. CONCLUSION: the count and the placements are computed in different places, and moving one without the other trades a real picture for a better number. The panel runs at line ~4949 and the Lloyd placement at ~7069, so k CAN reach it -- the fix is to re-place the region at the panel k, not to decrement the count."
  },
  "dense-board-overlap-peel": {
   "note": "ATTEMPT 4 on the dense board (142 for 120, all 120 pills found). A GREEDY OVERLAP PEEL -- drop the single deepest-penetrating placement, re-measure, repeat -- works beautifully in isolation: at a 10px floor it gives 124 for 120 with all 120 pills still found and 6 spurious, so the count error falls from +22 to +4 with ZERO real pills lost. A plain threshold cannot do this (real pills reach 24.68px penetration), only the greedy re-measurement can. It still cannot ship. Unscoped it costs 20 corpus images (239 -> 219). Scoped by \"packed fraction\" (share of placements deep inside a neighbour: dense board 0.27, shiny 0.06, real photos 0.00) at a 0.15 cut it costs 2 -- synth2-rw-kraft-small-n30-t35-s291 (30 -> 25) and synth2-rc-dark-small-n60-t65-s111 (60 -> 53), both DENSE SYNTHETICS whose pills are legitimately packed. The packed-fraction signature cannot distinguish a genuinely dense board from one whose partition invented extra hypotheses, because they look identical by that measure. THREE IMPLEMENTATION TRAPS worth keeping: the peel must run BEFORE `const out = {count,...}` (line ~8109) or the decrement is captured too late and does nothing; single-pill regions carry no g.pills so collecting only g.pills sees 2 of 129 nodes; and a multi-pill region may lose EVERY placement, so a \"never empty a region\" guard silently discarded 15 of 16 peeled nodes."
  }
 }
}
